---
"@ragpipe/plugin-voyage": minor
---

Add the new `@ragpipe/plugin-voyage` package for Voyage AI embeddings.

The initial release includes `voyageEmbedding()`, batch embedding support via `embedMany()`, response validation, tests, and package documentation.
